{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Orders list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Reduction</th>
                <th>Totalprice</th>
                <th>Date</th>
                <th>Payed</th>
                <th>Infoorder</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('orders_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.reduction }}</td>
                <td>{{ entity.totalPrice }}</td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.payed }}</td>
                <td>{{ entity.infoOrder }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('orders_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('orders_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('orders_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
