{% extends '::base.html.twig' %}

{% block body -%}
    <h1>OrderedProduct creation</h1>

    {% include('StoreBundle:Cart:index.html.twig') %}

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('orderedproduct') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
