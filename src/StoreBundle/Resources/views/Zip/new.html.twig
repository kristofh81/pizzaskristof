{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Zip creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('zip') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
